import { writeFileSync } from 'fs';
export default {
    /**
     * Every 1 minutes
     */
  
    '* * * * *': ({ strapi }) => {
        // For each contribution, check if it should change state

    },
  };